apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-db
  namespace: immich
spec:
  instances: 1
  storage:
    size: 10Gi
    storageClass: nfs
  bootstrap:
    initdb:
      database: immich
      owner: immich
      secret:
        name: immich-secrets