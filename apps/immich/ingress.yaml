apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: immich
  namespace: immich
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`immich.dozydelta.com`)
      kind: Rule
      services:
        - name: immich-server
          port: 2283
  tls:
    secretName: dozydelta-wildcard-tls